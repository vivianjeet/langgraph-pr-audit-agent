# Core agent framework
langgraph==1.2.2
langchain-core==1.4.0
# Durable checkpointing (opt-in --durable mode): persists graph threads to a SQLite file so a
# run paused for human review can resume across process restarts. Async saver, because the audit
# graph is driven by app.astream / aget_state (the async driver calls the async checkpoint methods).
langgraph-checkpoint-sqlite==3.1.0

# LLM + structured output
google-genai==2.7.0
# The [google-genai] extra pulls instructor's deps for that backend (e.g. jsonref) - bare
# `instructor` omits them, which silently breaks structured output on a clean install.
instructor[google-genai]==1.15.1

# Vector store
pgvector==0.4.2
psycopg[binary]==3.3.4

# Models / validation / retry / config
pydantic==2.13.4
tenacity==9.1.4
python-dotenv==1.2.2

# Compliance rule packs (scripts/seed_compliance.py loads packs/*.yaml). Declared directly
# rather than relied on transitively via langchain-core - a clean install / CI must not break
# if that transitive constraint ever changes.
PyYAML==6.0.3

# Observability (tracing + output-quality evaluators)
langsmith==0.8.6
langfuse==4.7.1

# Dev / test
pytest==9.0.3

# MCP
mcp==1.27.2
langchain-mcp-adapters==0.2.2